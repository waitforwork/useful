apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  namespace: prod
spec:
  containers:
  - name: redis
    image: redis:latest
    resources:
      requests:
        cpu: "100m"
        memory: "128Mi"
      limits:
        cpu: "100m"
        memory: "128Mi"
